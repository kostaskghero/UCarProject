<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="member">
 	<resultMap type="mvo" id="memberRM">
 	 	<result property="memberId" column="member_id"/>
 	 	<result property="memberPassword" column="member_password"/>
 	 	<result property="memberName" column="member_name"/>
 	 	<result property="memberEmail" column="member_email"/>
 	 	<result property="memberPhone" column="member_phone"/>
 	 	<result property="memberPoint" column="member_point"/>
 	 	<result property="memberLevel" column="member_level"/>
 	 	<result property="memberBirthDate" column="member_birth_date"/>
 	 </resultMap>
 	<insert id="registerMember" parameterType="mvo">
		insert into member(member_id, member_password, member_name, member_email, member_phone, member_birth_date)
		values(#{memberId}, #{memberPassword}, #{memberName}, #{memberEmail}, #{memberPhone}, #{memberBirthDate})
	</insert>
	<select id="loginMember" parameterType="mvo" resultMap="memberRM">
		select member_id, member_name from member where member_id=#{memberId} and member_password=#{memberPassword}

	</select>

</mapper> 
